
services:
  squid:
    build:
      context: ./squid
      args:
        PROXY_USER: squid
        PROXY_PASS: proxydoc32rom
    container_name: squid-proxy
    ports:
      - "3128:3128"
    restart: unless-stopped